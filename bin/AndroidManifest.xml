<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mms"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> 
        <activity android:name=".Patient"></activity>
        <activity android:name=".Register"></activity>
        <activity android:name=".Feedback"></activity>
        <activity android:name=".Update"></activity>
        <activity android:name=".Records"></activity>
        <activity android:name=".Book_app"></activity>
        <activity android:name=".SelectDoctor"></activity>
        <activity android:name=".Check"></activity>
    </application>

</manifest>
